server:
  port: 8080

telegram:
  bot:
    token:
    username: English_word_eugene

media:
  gif:
    start: https://gift-bucket-telegram.s3.eu-north-1.amazonaws.com/email2.mp4
    sheet-id: https://gift-bucket-telegram.s3.eu-north-1.amazonaws.com/link2.mp4

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bot_words
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  main:
    web-application-type: none
  ai:
    openai:
      api-key:
      chat:
        options:
          model: gpt-4o-mini

google:
  sheets:
    credentials-path: credentials.json

bot:
  secret_key:

